# Convex Backend

This directory contains your Convex backend functions.

## Getting Started

1. Run `bunx convex dev` to set up your Convex deployment
2. This will create the `_generated` folder with type-safe API definitions
3. Add your query, mutation, and action functions here

## Example Query

See `example.ts` for a sample query function.

